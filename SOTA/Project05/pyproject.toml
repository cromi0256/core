[project]
name = "project05"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "김영환", email = "cromi0256@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "polars>=1.44.1",
    "scikit-learn>=1.9.0",
]

[project.scripts]
project05 = "project05:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
